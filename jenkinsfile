pipeline {
    agent { label 'maven' }

    environment {
        PATH = "/opt/apache-maven-3.9.6/bin:$PATH"
    }
    stages {
        stage('Build-stage') {
            steps {
               sh 'mvn clean deploy'
            }
        }
    }
}

